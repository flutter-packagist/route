name: packagist_route
description: Wrapper for go_router.
version: 1.0.5
homepage: https://github.com/flutter-packagist/route

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3
  go_router: ^18.0.1
  log_wrapper: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
