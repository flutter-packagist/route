name: route
description: Wrapper for go_router.
version: 1.0.0
homepage: https://github.com/flutter-packagist/route

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: 4.6.5
  go_router: 10.0.0
  log_wrapper:
    path: ../log_wrapper

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
